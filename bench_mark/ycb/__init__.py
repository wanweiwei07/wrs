import os

dirname = os.path.dirname(__file__)
apple_file = os.path.join(dirname, 'apple.stl')
bowl_file = os.path.join(dirname, 'bowl.stl')
chips_can_file = os.path.join(dirname, 'chips_can.stl')
clamp_XS_file = os.path.join(dirname, 'clamp_XS.stl')
coffee_can_file = os.path.join(dirname, 'coffee_can.stl')
cracker_box_file = os.path.join(dirname, 'cracker_box.stl')
golfball_file = os.path.join(dirname, 'golfball.stl')
key_file = os.path.join(dirname, 'key.stl')
large_marker_file = os.path.join(dirname, 'large_marker.stl')
mini_soccer_ball_file = os.path.join(dirname, 'mini_soccer_ball.stl')
pear_800_file = os.path.join(dirname, 'pear_800.stl')
phillips_screwdriver_file = os.path.join(dirname, 'phillips_screwdriver.stl')
pitcher_file = os.path.join(dirname, 'pitcher.stl')
plate_file = os.path.join(dirname, 'plate.stl')
power_drill_file = os.path.join(dirname, 'power_drill.stl')
pudding_box_file = os.path.join(dirname, 'pudding_box.stl')
rope_blocks_file = os.path.join(dirname, 'rope_blocks.stl')
skillet_file = os.path.join(dirname, 'skillet.stl')
skillet_lid_file = os.path.join(dirname, 'skillet_lid.stl')
small_marker_file = os.path.join(dirname, 'small_marker.stl')
softball_file = os.path.join(dirname, 'softball.stl')
spatula_file = os.path.join(dirname, 'spatula.stl')
tuna_can_file = os.path.join(dirname, 'tuna_can.stl')
washer_10mm_file = os.path.join(dirname, 'washer_10mm.stl')
all_files = {"apple": apple_file,
             "bowl": bowl_file,
             "chips_can": chips_can_file,
             "clamp_XS": clamp_XS_file,
             "coffee_can": coffee_can_file,
             "cracker_box": cracker_box_file,
             "golfball": golfball_file,
             "key": key_file,
             "large_marker": large_marker_file,
             "mini_soccer_ball": mini_soccer_ball_file,
             "pear_800": pear_800_file,
             "phillips_screwdriver": phillips_screwdriver_file,
             "pitcher": pitcher_file,
             "plate": plate_file,
             "power_drill": power_drill_file,
             "pudding_box": pudding_box_file,
             "rope_blocks": rope_blocks_file,
             "skillet": skillet_file,
             "skillet_lid": skillet_lid_file,
             "small_marker": small_marker_file,
             "softball": softball_file,
             "spatula": spatula_file,
             "tuna_can": tuna_can_file,
             "washer_10mm": washer_10mm_file}
